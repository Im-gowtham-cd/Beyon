# Beyon Backend — Staging Environment
SPRING_PROFILES_ACTIVE=staging
SERVER_PORT=8080

# Database (Supabase — Staging Schema)
DB_HOST=aws-0-ap-northeast-1.pooler.supabase.com
DB_PORT=6543
DB_NAME=postgres
DB_USERNAME=postgres.wopozplrzhqjxvimtumz
DB_PASSWORD=${STAGING_DB_PASSWORD}

# Redis (Upstash — Staging Namespace)
REDIS_HOST=primary-impala-139794.upstash.io
REDIS_PORT=6379
REDIS_PASSWORD=${STAGING_REDIS_PASSWORD}

# JWT
JWT_SECRET=${STAGING_JWT_SECRET}
JWT_EXPIRATION=86400000

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:5173,https://staging.beyon.app

# Supabase Storage
SUPABASE_URL=${STAGING_SUPABASE_URL}
SUPABASE_KEY=${STAGING_SUPABASE_KEY}
